"""Runnable applications built on the kernel.

A package so the loader can name plugins as `examples.notes.providers:FileStore`,
which is the same dotted form any application uses.
"""

from __future__ import annotations
